
chan cA1 : {a0, a1};

initial E;

E  := a0 -> E0 | a0 -> E1;
E0 := a0 -> E0;
E1 := a1 -> E1;
E2 := b -> E2;
# Note on E2. It is a dummy state. It has been added so that we include a transition on b. 


# safety_spec = 'false'
# liveness_spec = 'FG(b=0)'
